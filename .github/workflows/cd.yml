name: CD

on:
  push:
    branches: [main]
    paths-ignore:
      # Don't trigger on Kargo's automated image tag updates
      - 'deploy/k8s/overlays/*/kustomization.yaml'

permissions:
  id-token: write    # OIDC for AWS
  contents: read

env:
  AWS_REGION: us-east-1
  # Update after running Terraform
  ECR_REGISTRY: ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com

jobs:
  # Only run CD if CI passed (reuse CI checks)
  build-and-push:
    name: Build & Push ${{ matrix.service }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - admin
          - router
          - worker-events
          - worker-email
          - worker-sms
          - worker-inbox
          - inbox
          - user
          - migrate
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::ACCOUNT_ID:role/hermes-github-actions
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        env:
          SERVICE: ${{ matrix.service }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build \
            --build-arg SERVICE="$SERVICE" \
            -t "${{ env.ECR_REGISTRY }}/hermes-${SERVICE}:${IMAGE_TAG}" \
            -t "${{ env.ECR_REGISTRY }}/hermes-${SERVICE}:latest" \
            -f deploy/docker/Dockerfile .
          docker push "${{ env.ECR_REGISTRY }}/hermes-${SERVICE}:${IMAGE_TAG}"
          docker push "${{ env.ECR_REGISTRY }}/hermes-${SERVICE}:latest"
