{{- $svcName := printf "%s-admin" (include "hermes.fullname" .) -}}
{{- $svcCtx := dict "root" . "service" "admin" -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $svcName }}
  labels:
    {{- include "hermes.serviceLabels" $svcCtx | nindent 4 }}
spec:
  {{- if not .Values.admin.autoscaling.enabled }}
  replicas: {{ .Values.admin.replicas }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "hermes.serviceSelectorLabels" $svcCtx | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- with .Values.admin.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "hermes.serviceSelectorLabels" $svcCtx | nindent 8 }}
    spec:
      {{- include "hermes.terminationGracePeriod" (dict "root" . "service" "admin") | nindent 6 }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
      {{- include "hermes.imagePullSecrets" . | nindent 6 }}
      containers:
        - name: admin
          image: {{ include "hermes.image" (dict "root" . "service" .Values.admin) }}
          imagePullPolicy: {{ include "hermes.imagePullPolicy" (dict "root" . "service" .Values.admin) }}
          ports:
            - name: http
              containerPort: {{ .Values.admin.port }}
              protocol: TCP
          env:
            - name: HERMES_HTTP_PORT
              value: {{ .Values.admin.port | quote }}
            # This service holds no NATS client, so there is nothing to drain and the
            # default budget would reserve 30s of the grace period for a step that never
            # runs. Stated rather than defaulted, so check_shutdown_budget.py measures the
            # sequence that actually happens.
            - name: HERMES_NATS_DRAIN_TIMEOUT
              value: "0s"
            {{- include "hermes.otelServiceEnv" $svcCtx | nindent 12 }}
            {{- include "hermes.rateLimitEnv" (dict "root" $ "svc" .Values.admin) | nindent 12 }}
            {{- if .Values.hermes.jwt.existingSecret }}
            - name: HERMES_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.hermes.jwt.existingSecret }}
                  key: {{ .Values.hermes.jwt.existingSecretKey }}
            {{- end }}
            {{- if .Values.hermes.apiKey.existingSecret }}
            - name: HERMES_API_KEY_HMAC_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.hermes.apiKey.existingSecret }}
                  key: {{ .Values.hermes.apiKey.existingSecretKey }}
            {{- end }}
            {{- if .Values.externalPostgresql.existingSecret }}
            - name: HERMES_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.externalPostgresql.existingSecret }}
                  key: {{ .Values.externalPostgresql.existingSecretKey }}
            {{- end }}
            {{- if .Values.externalNats.existingSecret }}
            - name: HERMES_NATS_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.externalNats.existingSecret }}
                  key: {{ .Values.externalNats.existingSecretKey | default "nats-url" }}
            {{- end }}
            {{- if .Values.externalRedis.existingSecret }}
            - name: HERMES_REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.externalRedis.existingSecret }}
                  key: {{ .Values.externalRedis.existingSecretKey }}
            {{- end }}
            {{- if .Values.externalCentrifugo.existingSecret }}
            - name: HERMES_CENTRIFUGO_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.externalCentrifugo.existingSecret }}
                  key: {{ .Values.externalCentrifugo.existingSecretKey }}
            {{- end }}
          envFrom:
            - configMapRef:
                name: {{ include "hermes.configMapName" . }}
            - secretRef:
                name: {{ include "hermes.secretName" . }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
            # One, not the default three: shutdown flips /readyz to 503 and then keeps
            # serving for the drain delay, so the pod must leave the Service endpoints on
            # the very next probe. Dependency blips are debounced in-process instead,
            # where the drain flag can bypass the debounce.
            failureThreshold: 1
          resources:
            {{- toYaml .Values.admin.resources | nindent 12 }}
          {{- include "hermes.preStop" . | nindent 10 }}
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 10Mi
      {{- with .Values.admin.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.admin.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.admin.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.admin.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $svcName }}
  labels:
    {{- include "hermes.serviceLabels" $svcCtx | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.admin.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hermes.serviceSelectorLabels" $svcCtx | nindent 4 }}
{{- if .Values.admin.autoscaling.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ $svcName }}
  labels:
    {{- include "hermes.serviceLabels" $svcCtx | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ $svcName }}
  minReplicas: {{ .Values.admin.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.admin.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.admin.autoscaling.targetCPUUtilizationPercentage }}
    {{- if .Values.admin.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.admin.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
