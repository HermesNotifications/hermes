# ArgoCD Application for Hermes staging environment.
# Syncs the staging Kustomize overlay automatically.
# Kargo controls WHEN image tags are updated in the overlay;
# ArgoCD just applies whatever is in git.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hermes-staging
  namespace: argocd
  labels:
    kargo.akuity.io/authorized-stage: staging
  annotations:
    kargo.akuity.io/authorized-stage: hermes:staging
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: git@github.com:darylrobbins/hermes.git
    targetRevision: main
    path: deploy/k8s/overlays/staging
  destination:
    server: https://kubernetes.default.svc
    namespace: hermes
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
