apiVersion: apps/v1
kind: Deployment
metadata:
  name: hermes-send
  annotations:
    # ADR 0006, amended 2026-07-31. Calls bootstrap.MustEnsureStreams and exits non-zero
    # without the JetStream streams, so it is applied only after wave 0 (NATS plus the
    # hermes-natsprovision Sync hook) is healthy. See services/dispatch.yaml for the
    # measurement and the cost.
    argocd.argoproj.io/sync-wave: "1"
  labels:
    app.kubernetes.io/name: hermes-send
    app.kubernetes.io/component: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: hermes-send
  template:
    metadata:
      labels:
        app.kubernetes.io/name: hermes-send
        app.kubernetes.io/component: api
    spec:
      serviceAccountName: hermes
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        fsGroup: 65534
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: send
          image: hermes-send
          ports:
            - name: http
              containerPort: 8088
              protocol: TCP
          envFrom:
            - configMapRef:
                name: hermes-config
            - secretRef:
                name: hermes-secrets
          env:
            - name: HERMES_HTTP_PORT
              value: "8088"
            - name: DD_SERVICE
              value: hermes-send
            - name: OTEL_SERVICE_NAME
              value: hermes-send
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            # ADR 0005 phase 2. The private CA that signs the NATS server certificate; no
            # system trust store has it. Only ca.crt is projected — the server's key and
            # certificate stay out of client pods. optional: true keeps the pod startable
            # in the local overlay, where NATS runs plaintext and this Secret does not
            # exist; HERMES_NATS_CA_BUNDLE is empty there, so nothing looks for the file.
            - name: nats-ca
              mountPath: /etc/nats-certs
              readOnly: true
            # ADR 0005 phase 3. This service's NKey seed — the private half of the identity
            # that selects its user, and so its subject permissions, in
            # deploy/k8s/base/infra/nats-accounts.conf. Only this service's key is
            # projected, so a compromised pod cannot read another service's seed out of its
            # own filesystem. optional: true for the same reason as the CA above: local NATS
            # has no accounts and HERMES_NATS_NKEY_SEED is empty there.
            - name: nats-nkey
              mountPath: /etc/nats-nkey
              readOnly: true
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 10Mi
        - name: nats-ca
          secret:
            secretName: nats-server-tls
            optional: true
            items:
              - key: ca.crt
                path: ca.crt
        - name: nats-nkey
          secret:
            secretName: nats-nkeys
            optional: true
            defaultMode: 0400
            items:
              - key: hermes-send.nk
                path: seed.nk
---
apiVersion: v1
kind: Service
metadata:
  name: hermes-send
spec:
  selector:
    app.kubernetes.io/name: hermes-send
  ports:
    - name: http
      port: 8088
      targetPort: http
