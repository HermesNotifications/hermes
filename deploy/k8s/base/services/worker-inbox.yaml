apiVersion: apps/v1
kind: Deployment
metadata:
  name: hermes-worker-inbox
  annotations:
    # ADR 0006, amended 2026-07-31. Calls bootstrap.MustEnsureStreams and exits non-zero
    # without the JetStream streams, so it is applied only after wave 0 (NATS plus the
    # hermes-natsprovision Sync hook) is healthy. See services/dispatch.yaml for the
    # measurement and the cost.
    argocd.argoproj.io/sync-wave: "1"
  labels:
    app.kubernetes.io/name: hermes-worker-inbox
    app.kubernetes.io/component: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: hermes-worker-inbox
  template:
    metadata:
      labels:
        app.kubernetes.io/name: hermes-worker-inbox
        app.kubernetes.io/component: worker
    spec:
      serviceAccountName: hermes
      # 60, not 30, because this pod drains NATS on the way out: 5s readiness drain + up to 30s
      # waiting for in-flight handlers + 15s HTTP shutdown = 50s. Under a 30s grace the kubelet
      # would SIGKILL partway through, abandoning a delivery it had already started — which is
      # worse than not draining, since the message is then redelivered and re-sent.
      # scripts/check_shutdown_budget.py enforces the arithmetic.
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        fsGroup: 65534
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: worker-inbox
          image: hermes-worker-inbox
          ports:
            - name: http
              containerPort: 8085
              protocol: TCP
          envFrom:
            - configMapRef:
                name: hermes-config
            - secretRef:
                name: hermes-secrets
          env:
            - name: HERMES_HTTP_PORT
              value: "8085"
            - name: DD_SERVICE
              value: hermes-worker-inbox
            - name: OTEL_SERVICE_NAME
              value: hermes-worker-inbox
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            # ADR 0005 phase 2. The private CA that signs the NATS server certificate; no
            # system trust store has it. Only ca.crt is projected — the server's key and
            # certificate stay out of client pods. optional: true keeps the pod startable
            # in the local overlay, where NATS runs plaintext and this Secret does not
            # exist; HERMES_NATS_CA_BUNDLE is empty there, so nothing looks for the file.
            - name: nats-ca
              mountPath: /etc/nats-certs
              readOnly: true
            # ADR 0005 phase 3. Only this service's NKey seed is projected — see send.yaml.
            - name: nats-nkey
              mountPath: /etc/nats-nkey
              readOnly: true
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 10Mi
        - name: nats-ca
          secret:
            secretName: nats-server-tls
            optional: true
            items:
              - key: ca.crt
                path: ca.crt
        - name: nats-nkey
          secret:
            secretName: nats-nkeys
            optional: true
            defaultMode: 0400
            items:
              - key: hermes-worker-inbox.nk
                path: seed.nk
---
apiVersion: v1
kind: Service
metadata:
  name: hermes-worker-inbox
spec:
  selector:
    app.kubernetes.io/name: hermes-worker-inbox
  ports:
    - name: http
      port: 8085
      targetPort: http
