apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - default-deny.yaml
  - allow-ingress.yaml
  - allow-nats.yaml
  - allow-egress-managed.yaml
  # Only in this overlay — the generators reach the services from another namespace
  # rather than through ingress-nginx. See the file for the full chain.
  - allow-loadtest-generators.yaml
