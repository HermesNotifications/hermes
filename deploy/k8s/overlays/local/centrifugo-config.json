{
  "engine": "redis",
  "redis_address": "redis:6379",
  "broker": "nats",
  "nats_url": "nats://nats:4222",
  "token_hmac_secret_key": "dev-jwt-secret",
  "api_key": "centrifugo-api-key",
  "admin": true,
  "http_stream": true,
  "sse": true,
  "presence": true,
  "history_size": 50,
  "history_ttl": "1h",
  "user_subscribe_to_personal": true,
  "allow_user_limited_channels": true,
  "_comment_allowed_origins": "Local dev only. The demo's port moves per worktree (see scripts/demo-env) and a per-worker sandbox is reached at <worker>.127.0.0.1.nip.io, so a fixed origin would have to be regenerated per sandbox. Staging and production patch this with their real origins and are not affected.",
  "allowed_origins": ["http://localhost:*", "http://127.0.0.1:*", "http://*.127.0.0.1.nip.io:*"],
  "health": true
}
