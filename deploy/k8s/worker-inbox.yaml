apiVersion: apps/v1
kind: Deployment
metadata:
  name: hermes-worker-inbox
  namespace: hermes
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hermes-worker-inbox
  template:
    metadata:
      labels:
        app: hermes-worker-inbox
    spec:
      containers:
        - name: worker-inbox
          image: hermes-worker-inbox:latest
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: hermes-config
            - secretRef:
                name: hermes-secrets
          env:
            - name: HERMES_HTTP_PORT
              value: "8085"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8085
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8085
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: hermes-worker-inbox
  namespace: hermes
spec:
  selector:
    app: hermes-worker-inbox
  ports:
    - port: 8085
      targetPort: 8085
