apiVersion: apps/v1
kind: Deployment
metadata:
  name: hermes-worker-events
  namespace: hermes
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hermes-worker-events
  template:
    metadata:
      labels:
        app: hermes-worker-events
    spec:
      containers:
        - name: worker-events
          image: hermes-worker-events:latest
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: hermes-config
            - secretRef:
                name: hermes-secrets
          env:
            - name: HERMES_HTTP_PORT
              value: "8082"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8082
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8082
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: hermes-worker-events
  namespace: hermes
spec:
  selector:
    app: hermes-worker-events
  ports:
    - port: 8082
      targetPort: 8082
