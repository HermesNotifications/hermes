apiVersion: apps/v1
kind: Deployment
metadata:
  name: hermes-inbox
  namespace: hermes
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hermes-inbox
  template:
    metadata:
      labels:
        app: hermes-inbox
    spec:
      containers:
        - name: inbox
          image: hermes-inbox:latest
          ports:
            - containerPort: 8086
          envFrom:
            - configMapRef:
                name: hermes-config
            - secretRef:
                name: hermes-secrets
          env:
            - name: HERMES_HTTP_PORT
              value: "8086"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8086
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8086
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: hermes-inbox
  namespace: hermes
spec:
  selector:
    app: hermes-inbox
  ports:
    - port: 8086
      targetPort: 8086
