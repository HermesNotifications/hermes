openapi: 3.1.0
info:
  description: "Server-to-server API for managing subscription categories, templates,\
    \ and notifications."
  title: Hermes Admin API
  version: 1.0.0
servers:
- url: /
paths:
  /v1/apikeys:
    get:
      operationId: list-api-keys
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/ApiKeyView"
                nullable: true
                type: array
          description: OK
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List all API keys
      tags:
      - API Keys
      x-accepts:
      - application/json
      - application/problem+json
    post:
      operationId: create-api-key
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAPIKeyInputBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiKeyCreatedOutputBody"
          description: Created
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Create a new API key
      tags:
      - API Keys
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
  /v1/apikeys/{id}:
    delete:
      operationId: delete-api-key
      parameters:
      - description: API key ID
        explode: false
        in: path
        name: id
        required: true
        schema:
          description: API key ID
          type: string
        style: simple
      responses:
        "204":
          description: No Content
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Revoke an API key
      tags:
      - API Keys
      x-accepts:
      - application/json
      - application/problem+json
  /v1/apikeys/{id}/rate-limit:
    put:
      description: "Replaces this key's rate limit. Omitted fields reset to the service\
        \ default, so an empty body clears the override entirely. Takes effect within\
        \ the API key cache TTL; this endpoint invalidates that entry so the change\
        \ is immediate."
      operationId: set-api-key-rate-limit
      parameters:
      - description: API key ID
        explode: false
        in: path
        name: id
        required: true
        schema:
          description: API key ID
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetAPIKeyRateLimitInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiKeyView"
          description: OK
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Set or clear a key's rate limit
      tags:
      - API Keys
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
  /v1/auth/token:
    post:
      operationId: exchange-token
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TokenInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenOutputBody"
          description: OK
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Exchange credentials for a user JWT token
      tags:
      - Auth
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
  /v1/notifications:
    get:
      operationId: list-notifications
      parameters:
      - description: Max results (default 50)
        explode: false
        in: query
        name: limit
        required: false
        schema:
          description: Max results (default 50)
          format: int64
          maximum: 200
          minimum: 1
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/NotificationItem"
                nullable: true
                type: array
          description: OK
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List recent notifications
      tags:
      - Notifications
      x-accepts:
      - application/json
      - application/problem+json
  /v1/notifications/{id}:
    get:
      operationId: get-notification
      parameters:
      - description: Notification ID
        explode: false
        in: path
        name: id
        required: true
        schema:
          description: Notification ID
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationStatusOutputBody"
          description: OK
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Get notification status and events
      tags:
      - Notifications
      x-accepts:
      - application/json
      - application/problem+json
  /v1/organizations:
    get:
      operationId: list-organizations
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/OrganizationItem"
                nullable: true
                type: array
          description: OK
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List organizations
      tags:
      - Organizations
      x-accepts:
      - application/json
      - application/problem+json
    post:
      operationId: create-organization
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOrganizationInputBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationItem"
          description: Created
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Create an organization
      tags:
      - Organizations
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
  /v1/send:
    post:
      operationId: send-notification
      parameters:
      - description: Idempotency key for deduplication
        explode: false
        in: header
        name: X-Idempotency-Key
        required: false
        schema:
          description: Idempotency key for deduplication
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendInputBody"
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendOutputBody"
          description: Accepted
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Send a notification
      tags:
      - Notifications
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
  /v1/subscriptions/categories:
    get:
      operationId: list-subscription-categories
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/SubscriptionCategory"
                nullable: true
                type: array
          description: OK
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List subscription categories
      tags:
      - Subscriptions
      x-accepts:
      - application/json
      - application/problem+json
    post:
      operationId: create-subscription-category
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCategoryInputBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionCategory"
          description: Created
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Create a subscription category
      tags:
      - Subscriptions
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
  /v1/subscriptions/categories/{category_id}/subscriptions:
    get:
      operationId: list-subscriptions
      parameters:
      - description: Category ID
        explode: false
        in: path
        name: category_id
        required: true
        schema:
          description: Category ID
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/Subscription"
                nullable: true
                type: array
          description: OK
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List subscriptions in a category
      tags:
      - Subscriptions
      x-accepts:
      - application/json
      - application/problem+json
    post:
      operationId: create-subscription
      parameters:
      - description: Category ID
        explode: false
        in: path
        name: category_id
        required: true
        schema:
          description: Category ID
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSubscriptionInputBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Subscription"
          description: Created
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Create a subscription
      tags:
      - Subscriptions
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
  /v1/subscriptions/categories/{id}:
    delete:
      operationId: delete-subscription-category
      parameters:
      - description: Category ID
        explode: false
        in: path
        name: id
        required: true
        schema:
          description: Category ID
          type: string
        style: simple
      responses:
        "204":
          description: No Content
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Delete a subscription category
      tags:
      - Subscriptions
      x-accepts:
      - application/json
      - application/problem+json
    put:
      operationId: update-subscription-category
      parameters:
      - description: Category ID
        explode: false
        in: path
        name: id
        required: true
        schema:
          description: Category ID
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCategoryInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionCategory"
          description: OK
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Update a subscription category
      tags:
      - Subscriptions
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
  /v1/subscriptions/{id}:
    delete:
      operationId: delete-subscription
      parameters:
      - description: Subscription ID
        explode: false
        in: path
        name: id
        required: true
        schema:
          description: Subscription ID
          type: string
        style: simple
      responses:
        "204":
          description: No Content
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Delete a subscription
      tags:
      - Subscriptions
      x-accepts:
      - application/json
      - application/problem+json
    put:
      operationId: update-subscription
      parameters:
      - description: Subscription ID
        explode: false
        in: path
        name: id
        required: true
        schema:
          description: Subscription ID
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSubscriptionInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Subscription"
          description: OK
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Update a subscription
      tags:
      - Subscriptions
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
  /v1/templates:
    get:
      operationId: list-templates
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/NotificationTemplate"
                nullable: true
                type: array
          description: OK
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List notification templates
      tags:
      - Templates
      x-accepts:
      - application/json
      - application/problem+json
    post:
      operationId: create-template
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTemplateInputBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationTemplate"
          description: Created
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Create a notification template
      tags:
      - Templates
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
  /v1/templates/{id}:
    delete:
      operationId: delete-template
      parameters:
      - description: Template ID
        explode: false
        in: path
        name: id
        required: true
        schema:
          description: Template ID
          type: string
        style: simple
      responses:
        "204":
          description: No Content
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Delete a notification template
      tags:
      - Templates
      x-accepts:
      - application/json
      - application/problem+json
    put:
      operationId: update-template
      parameters:
      - description: Template ID
        explode: false
        in: path
        name: id
        required: true
        schema:
          description: Template ID
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTemplateInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationTemplate"
          description: OK
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: Update a notification template
      tags:
      - Templates
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
  /v1/users:
    get:
      operationId: list-users
      parameters:
      - description: Filter by organization ID
        explode: false
        in: query
        name: organization_id
        required: false
        schema:
          description: Filter by organization ID
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/UserItem"
                nullable: true
                type: array
          description: OK
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/list_api_keys_429_response"
          description: "Too Many Requests. The caller exceeded its rate limit. Honour\
            \ Retry-After; retrying sooner does not shorten the wait. A 429 from the\
            \ pre-authentication per-address bound carries only Retry-After, without\
            \ the RateLimit-* headers."
          headers:
            RateLimit-Limit:
              description: Sustained requests per second allowed for this credential.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Remaining:
              description: Requests available right now.
              explode: false
              schema:
                type: integer
              style: simple
            RateLimit-Reset:
              description: Seconds until capacity is available.
              explode: false
              schema:
                type: integer
              style: simple
            Retry-After:
              description: Whole seconds to wait before retrying. Always at least
                1.
              explode: false
              schema:
                type: integer
              style: simple
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: List users
      tags:
      - Users
      x-accepts:
      - application/json
      - application/problem+json
components:
  schemas:
    ApiKeyCreatedOutputBody:
      additionalProperties: false
      example:
        $schema: https://openapi-generator.tech
        rate_limit_burst: 0
        permissions:
        - permissions
        - permissions
        rate_limit_per_second: 6
        name: name
        created_at: 2000-01-23T04:56:07.000+00:00
        id: id
        raw_key: raw_key
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        created_at:
          format: date-time
          type: string
        id:
          type: string
        name:
          type: string
        permissions:
          items:
            type: string
          nullable: true
          type: array
        rate_limit_burst:
          format: int64
          type: integer
        rate_limit_per_second:
          format: int64
          type: integer
        raw_key:
          type: string
      required:
      - created_at
      - id
      - name
      - permissions
      - raw_key
    ApiKeyView:
      additionalProperties: false
      example:
        $schema: https://openapi-generator.tech
        rate_limit_burst: 0
        permissions:
        - permissions
        - permissions
        rate_limit_per_second: 6
        name: name
        created_at: 2000-01-23T04:56:07.000+00:00
        id: id
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        created_at:
          format: date-time
          type: string
        id:
          type: string
        name:
          type: string
        permissions:
          items:
            type: string
          nullable: true
          type: array
        rate_limit_burst:
          description: Absent means the service default applies.
          format: int64
          type: integer
        rate_limit_per_second:
          description: Absent means the service default applies.
          format: int64
          type: integer
      required:
      - created_at
      - id
      - name
      - permissions
    CreateAPIKeyInputBody:
      additionalProperties: false
      example:
        $schema: https://openapi-generator.tech
        rate_limit_burst: 1
        permissions:
        - permissions
        - permissions
        rate_limit_per_second: 1
        name: name
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        name:
          description: Human-readable key name
          minLength: 1
          type: string
        permissions:
          description: Permission set (defaults to all except apikeys:manage)
          items:
            type: string
          nullable: true
          type: array
        rate_limit_burst:
          description: Requests admitted instantaneously for this key. Omit to use
            the service default.
          format: int64
          minimum: 1
          type: integer
        rate_limit_per_second:
          description: Sustained requests per second for this key. Omit to use the
            service default.
          format: int64
          minimum: 1
          type: integer
      required:
      - name
    CreateCategoryInputBody:
      additionalProperties: false
      example:
        $schema: https://openapi-generator.tech
        default_state: "on"
        default_channels:
        - default_channels
        - default_channels
        name: name
        sort_order: 0
        slug: slug
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        default_channels:
          description: Default delivery channels
          items:
            type: string
          nullable: true
          type: array
        default_state:
          description: Default subscription state
          enum:
          - "on"
          - "off"
          - required
          type: string
        name:
          description: Human-readable name
          minLength: 1
          type: string
        slug:
          description: URL-friendly identifier
          minLength: 1
          type: string
        sort_order:
          description: Display order
          format: int64
          type: integer
      required:
      - default_state
      - name
      - slug
    CreateOrganizationInputBody:
      additionalProperties: false
      example:
        $schema: https://openapi-generator.tech
        name: name
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        name:
          description: Organization name
          minLength: 1
          type: string
      required:
      - name
    CreateSubscriptionInputBody:
      additionalProperties: false
      example:
        $schema: https://openapi-generator.tech
        name: name
        sort_order: 0
        slug: slug
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        name:
          description: Human-readable name
          minLength: 1
          type: string
        slug:
          description: URL-friendly identifier
          minLength: 1
          type: string
        sort_order:
          description: Display order within category
          format: int64
          type: integer
      required:
      - name
      - slug
    CreateTemplateInputBody:
      additionalProperties: false
      example:
        subscription_id: subscription_id
        $schema: https://openapi-generator.tech
        default_channels:
        - default_channels
        - default_channels
        name: name
        content:
          key:
            key: content
        slug: slug
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        content:
          additionalProperties:
            additionalProperties:
              type: string
          description: "Per-channel content: channel slug -> field key -> template\
            \ string (e.g. {\"email\":{\"subject\":\"...\",\"body\":\"...\"}})"
        default_channels:
          description: Default channels (used when no subscription)
          items:
            type: string
          nullable: true
          type: array
        name:
          description: Human-readable name
          minLength: 1
          type: string
        slug:
          description: URL-friendly identifier
          minLength: 1
          type: string
        subscription_id:
          description: Subscription ID (null for standalone)
          type: string
      required:
      - name
      - slug
    ErrorDetail:
      additionalProperties: false
      properties:
        location:
          description: "Where the error occurred, e.g. 'body.items[3].tags' or 'path.thing-id'"
          type: string
        message:
          description: Error message text
          type: string
        value: {}
    ErrorModel:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        detail:
          description: A human-readable explanation specific to this occurrence of
            the problem.
          type: string
        errors:
          description: Optional list of individual error details
          items:
            $ref: "#/components/schemas/ErrorDetail"
          nullable: true
          type: array
        instance:
          description: A URI reference that identifies the specific occurrence of
            the problem.
          format: uri
          type: string
        status:
          description: HTTP status code
          format: int64
          type: integer
        title:
          description: "A short, human-readable summary of the problem type. This\
            \ value should not change between occurrences of the error."
          type: string
        type:
          default: about:blank
          description: A URI reference to human-readable documentation for the error.
          format: uri
          type: string
    Notification:
      additionalProperties: false
      properties:
        action_label:
          type: string
        action_url:
          type: string
        archived_at:
          format: date-time
          type: string
        body:
          type: string
        category_id:
          type: string
        channels:
          items:
            type: string
          nullable: true
          type: array
        created_at:
          format: date-time
          type: string
        deleted_at:
          format: date-time
          type: string
        delivered_at:
          format: date-time
          type: string
        id:
          type: string
        idempotency_key:
          type: string
        organization_id:
          type: string
        read_at:
          format: date-time
          type: string
        sent_at:
          format: date-time
          type: string
        status:
          type: string
        template_id:
          type: string
        title:
          type: string
        user_id:
          type: string
      required:
      - body
      - category_id
      - channels
      - created_at
      - id
      - organization_id
      - status
      - title
      - user_id
    NotificationEvent:
      additionalProperties: false
      example:
        severity: severity
        metadata: ""
        channel: channel
        created_at: 2000-01-23T04:56:07.000+00:00
        id: id
        notification_id: notification_id
        event: event
      properties:
        channel:
          type: string
        created_at:
          format: date-time
          type: string
        event:
          type: string
        id:
          type: string
        metadata: {}
        notification_id:
          type: string
        severity:
          type: string
      required:
      - channel
      - created_at
      - event
      - id
      - notification_id
      - severity
    NotificationItem:
      additionalProperties: false
      example:
        category_id: category_id
        channels:
        - channels
        - channels
        template_slug: template_slug
        user_id: user_id
        organization_id: organization_id
        created_at: 2000-01-23T04:56:07.000+00:00
        template_id: template_id
        id: id
        body: body
        title: title
        status: status
      properties:
        body:
          type: string
        category_id:
          type: string
        channels:
          items:
            type: string
          nullable: true
          type: array
        created_at:
          format: date-time
          type: string
        id:
          type: string
        organization_id:
          type: string
        status:
          type: string
        template_id:
          type: string
        template_slug:
          type: string
        title:
          type: string
        user_id:
          type: string
      required:
      - body
      - category_id
      - channels
      - created_at
      - id
      - organization_id
      - status
      - title
      - user_id
    NotificationStatusOutputBody:
      additionalProperties: false
      example:
        notification: ""
        $schema: https://openapi-generator.tech
        events:
        - severity: severity
          metadata: ""
          channel: channel
          created_at: 2000-01-23T04:56:07.000+00:00
          id: id
          notification_id: notification_id
          event: event
        - severity: severity
          metadata: ""
          channel: channel
          created_at: 2000-01-23T04:56:07.000+00:00
          id: id
          notification_id: notification_id
          event: event
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        events:
          description: Timeline of notification events
          items:
            $ref: "#/components/schemas/NotificationEvent"
          nullable: true
          type: array
        notification:
          allOf:
          - $ref: "#/components/schemas/Notification"
          description: The notification record
      required:
      - events
      - notification
    NotificationTemplate:
      additionalProperties: false
      example:
        subscription_id: subscription_id
        $schema: https://openapi-generator.tech
        default_channels:
        - default_channels
        - default_channels
        name: name
        created_at: 2000-01-23T04:56:07.000+00:00
        id: id
        content:
          key:
            key: content
        slug: slug
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        content:
          additionalProperties:
            additionalProperties:
              type: string
        created_at:
          format: date-time
          type: string
        default_channels:
          items:
            type: string
          nullable: true
          type: array
        id:
          type: string
        name:
          type: string
        slug:
          type: string
        subscription_id:
          type: string
      required:
      - created_at
      - default_channels
      - id
      - name
      - slug
    OrganizationItem:
      additionalProperties: false
      example:
        $schema: https://openapi-generator.tech
        user_count: 0
        name: name
        created_at: 2000-01-23T04:56:07.000+00:00
        default_locale: default_locale
        id: id
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        created_at:
          format: date-time
          type: string
        default_locale:
          type: string
        id:
          type: string
        name:
          type: string
        user_count:
          format: int64
          type: integer
      required:
      - created_at
      - default_locale
      - id
      - name
      - user_count
    SendContent:
      additionalProperties: false
      properties:
        action_label:
          description: Optional action button label
          type: string
        action_url:
          description: Optional action URL
          type: string
        body:
          description: Notification body
          type: string
        title:
          description: Notification title
          type: string
      required:
      - body
      - title
    SendInputBody:
      additionalProperties: false
      example:
        template: template
        $schema: https://openapi-generator.tech
        channels:
        - channels
        - channels
        data:
          key: ""
        to: ""
        content: ""
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        channels:
          description: Explicit delivery channels
          items:
            type: string
          nullable: true
          type: array
        content:
          allOf:
          - $ref: "#/components/schemas/SendContent"
          description: Direct content (mutually exclusive with template)
        data:
          additionalProperties: {}
          description: Template data for rendering
        template:
          description: Notification template slug (mutually exclusive with content)
          type: string
        to:
          allOf:
          - $ref: "#/components/schemas/SendRecipient"
          description: Notification recipient
      required:
      - to
    SendOutputBody:
      additionalProperties: false
      example:
        $schema: https://openapi-generator.tech
        notification_id: notification_id
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        notification_id:
          description: ID of the created notification
          type: string
      required:
      - notification_id
    SendRecipient:
      additionalProperties: false
      properties:
        contacts:
          additionalProperties:
            type: string
          description: "Per-channel address overrides: address key (\"email\",\"phone\"\
            ) -> address"
        organization_id:
          description: Organization identifier
          minLength: 1
          type: string
        user_id:
          description: External user identifier
          minLength: 1
          type: string
      required:
      - organization_id
      - user_id
    SetAPIKeyRateLimitInputBody:
      additionalProperties: false
      example:
        $schema: https://openapi-generator.tech
        per_second: 1
        burst: 1
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        burst:
          description: Requests admitted instantaneously. Omit to reset to the service
            default.
          format: int64
          minimum: 1
          type: integer
        per_second:
          description: Sustained requests per second. Omit to reset to the service
            default.
          format: int64
          minimum: 1
          type: integer
    Subscription:
      additionalProperties: false
      example:
        $schema: https://openapi-generator.tech
        category_id: category_id
        name: name
        created_at: 2000-01-23T04:56:07.000+00:00
        id: id
        sort_order: 0
        slug: slug
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        category_id:
          type: string
        created_at:
          format: date-time
          type: string
        id:
          type: string
        name:
          type: string
        slug:
          type: string
        sort_order:
          format: int64
          type: integer
      required:
      - category_id
      - created_at
      - id
      - name
      - slug
      - sort_order
    SubscriptionCategory:
      additionalProperties: false
      example:
        $schema: https://openapi-generator.tech
        default_state: default_state
        default_channels:
        - default_channels
        - default_channels
        name: name
        created_at: 2000-01-23T04:56:07.000+00:00
        id: id
        sort_order: 0
        slug: slug
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        created_at:
          format: date-time
          type: string
        default_channels:
          items:
            type: string
          nullable: true
          type: array
        default_state:
          type: string
        id:
          type: string
        name:
          type: string
        slug:
          type: string
        sort_order:
          format: int64
          type: integer
      required:
      - created_at
      - default_channels
      - default_state
      - id
      - name
      - slug
      - sort_order
    TokenInputBody:
      additionalProperties: false
      example:
        $schema: https://openapi-generator.tech
        user_id: user_id
        organization_id: organization_id
        expires_in: 51745
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        expires_in:
          description: "Requested token lifetime in seconds (min 3600 = 1h, max 604800\
            \ = 7d, default 14400 = 4h). The actual expiry includes ±10% random jitter\
            \ to prevent thundering-herd token refreshes."
          format: int64
          maximum: 604800
          minimum: 3600
          type: integer
        organization_id:
          description: Organization identifier
          minLength: 1
          type: string
        user_id:
          description: External user identifier
          minLength: 1
          type: string
      required:
      - organization_id
      - user_id
    TokenOutputBody:
      additionalProperties: false
      example:
        expires_at: expires_at
        $schema: https://openapi-generator.tech
        token: token
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        expires_at:
          description: Token expiration time in RFC3339 format
          type: string
        token:
          description: JWT token for user-facing API access
          type: string
      required:
      - expires_at
      - token
    UpdateCategoryInputBody:
      additionalProperties: false
      example:
        $schema: https://openapi-generator.tech
        default_state: "on"
        default_channels:
        - default_channels
        - default_channels
        name: name
        sort_order: 0
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        default_channels:
          description: Default delivery channels
          items:
            type: string
          nullable: true
          type: array
        default_state:
          description: Default subscription state
          enum:
          - "on"
          - "off"
          - required
          type: string
        name:
          description: Human-readable name
          type: string
        sort_order:
          description: Display order
          format: int64
          type: integer
      required:
      - default_channels
      - default_state
      - name
      - sort_order
    UpdateSubscriptionInputBody:
      additionalProperties: false
      example:
        $schema: https://openapi-generator.tech
        name: name
        sort_order: 0
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        name:
          description: Human-readable name
          type: string
        sort_order:
          description: Display order within category
          format: int64
          type: integer
      required:
      - name
      - sort_order
    UpdateTemplateInputBody:
      additionalProperties: false
      example:
        subscription_id: subscription_id
        $schema: https://openapi-generator.tech
        default_channels:
        - default_channels
        - default_channels
        name: name
        content:
          key:
            key: content
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          format: uri
          readOnly: true
          type: string
        content:
          additionalProperties:
            additionalProperties:
              type: string
          description: "Per-channel content: channel slug -> field key -> template\
            \ string (e.g. {\"email\":{\"subject\":\"...\",\"body\":\"...\"}})"
        default_channels:
          description: Default channels (used when no subscription)
          items:
            type: string
          nullable: true
          type: array
        name:
          description: Human-readable name
          type: string
        subscription_id:
          description: Subscription ID (null for standalone)
          type: string
      required:
      - name
    UserItem:
      additionalProperties: false
      example:
        organization_id: organization_id
        created_at: 2000-01-23T04:56:07.000+00:00
        external_id: external_id
        id: id
        organization_name: organization_name
        locale: locale
        contacts:
          key: contacts
      properties:
        contacts:
          additionalProperties:
            type: string
        created_at:
          format: date-time
          type: string
        external_id:
          type: string
        id:
          type: string
        locale:
          nullable: true
          type: string
        organization_id:
          type: string
        organization_name:
          type: string
      required:
      - created_at
      - external_id
      - id
      - locale
      - organization_id
      - organization_name
    list_api_keys_429_response:
      example:
        error: error
      properties:
        error:
          description: Human-readable reason.
          type: string

